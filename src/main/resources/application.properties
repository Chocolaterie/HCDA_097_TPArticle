spring.application.name=tp_article

# Param bdd (SQL)
spring.datasource.url=jdbc:mysql://localhost:3306/db_article?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password

# Recreer le schema
spring.jpa.hibernate.ddl-auto=create

# On ne recreer le schema mais on inster les données
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Afficher les requetes
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Localization
spring.messages.basename=messages
spring.messages.encoding=UTF-8

app.jwt.secret=69636e783529213d5722613b2b336c793371666524684a3445226e5573